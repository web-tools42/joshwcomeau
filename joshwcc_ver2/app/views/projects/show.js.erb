htmlCode = "<%= escape_javascript(render :partial => 'projects/show', locals: {project: @project}).html_safe %>";
elem = angular.element(".active-project-container");
$compile = elem.injector().get('$compile');
scope = elem.scope();
elem.html( $compile(htmlCode)(scope) );
scope.$digest();